.dcloud_form
  %h2
    Upload New Version of #{h @artifact.name}
  - form_for(:artifact_version, :url => artifact_artifact_versions_path(@artifact), :html => {:multipart => true}) do |f|
    = f.error_messages
    = render :partial => "form", :object => f
    = f.submit 'Upload', :class => "submit"
  %p
    = link_to 'Cancel', @artifact, :class => "actionlink"
