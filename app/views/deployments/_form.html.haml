= form.label :app_version, "Application Version"
%br
= form.select :app_version_id, current_user.app_versions.all.collect { |av| [av.to_s, av.id ] }
%br
%br
= form.label :environment
%br
= form.select :environment_id, current_user.environments.all.collect { |ev| [ev.name, ev.id ] }
%br
%br
= form.label :datasource
%br
= form.radio_button :datasource, "local"
Local
%br
= form.radio_button :datasource, "cloud"
Cloud
%br
%br