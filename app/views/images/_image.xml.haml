%image{:href=>environment_image_url(@environment, image), :id=>image.friendly_id, :'proxy-for'=>@environment.cloud.image(image.uid).url}
  %owner steamcannon
  %name=image.name
  %description=image.description
  %architecture=cloud_image.architecture

