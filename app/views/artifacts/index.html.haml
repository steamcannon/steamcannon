#artifacts_index
  .page_header
    .actions
      = link_to 'Add Artifact', new_artifact_path
    %h1 Artifacts
  = render :partial => "list", :locals => { :artifacts => @artifacts, :empty_text => "You have no artifacts." }

